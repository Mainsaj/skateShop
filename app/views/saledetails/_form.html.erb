<%= form_with(model: saledetail, local: true) do |form| %>
  <% if saledetail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saledetail.errors.count, "error") %> prohibited this saledetail from being saved:</h2>

      <ul>
      <% saledetail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sail_id %>
    <%= form.number_field :sail_id, id: :saledetail_sail_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :saledetail_product_id %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, id: :saledetail_cost %>
  </div>

  <div class="field">
    <%= form.label :prodqty %>
    <%= form.number_field :prodqty, id: :saledetail_prodqty %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
